# Legacy source snapshots

这里保存早期整理阶段使用的原始来源快照，不参与当前构建、校验或浏览器运行：

- `speculative_decoding_papers_2026-07.xlsx`：Excel A–I 列原始论文信息；
- `tag.txt`：旧 D / Q / T / V / R / S / H 标签及逐论文贡献。

当前维护入口是 `../data/catalog.json` 与 `../data/papers/*.json`；
`../data/catalog.generated.json` 和 `../assets/js/data.js` 均由同步脚本生成。
这两个快照只用于历史追溯，不再约束新增论文或 A–E 子问题模型。
